import 'package:flutter/cupertino.dart';

class ImageUploadedCard extends StatelessWidget {
  const ImageUploadedCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
